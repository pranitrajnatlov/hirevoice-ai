# Gateway base URL the Next.js rewrite proxies /api/v1 to (server-side).
GATEWAY_URL=http://localhost:8000

# Optional: absolute API base for client fetches (leave empty to use same-origin /api/v1 proxy).
NEXT_PUBLIC_API_URL=
