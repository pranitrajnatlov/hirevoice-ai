# HireVoice AI Service — FastAPI wrapper deps.
# Install alongside the repo-root requirements.txt (which holds the AI core deps:
# faster-whisper, piper-tts, ollama, openai, etc.):
#   pip install -r services/ai/requirements.txt -r requirements.txt

fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9       # UploadFile support
pydantic>=2.0
httpx>=0.27.0
