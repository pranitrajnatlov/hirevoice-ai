# HireVoice AI — Phase 1 dependencies
# Install: pip install -r requirements.txt

# UI
gradio>=4.44.0

# STT (local)
faster-whisper>=1.0.3
ctranslate2>=4.0.0

# Audio I/O
soundfile>=0.12.1
numpy>=1.26.0

# TTS (local) — piper-tts or CLI fallback
piper-tts>=1.2.0

# Ollama Python client
ollama>=0.4.0

# Resource monitoring
psutil>=5.9.0

# OpenAI mode (Phase 4 — optional)
openai>=1.40.0

# Resume parsing (Phase 2 — optional)
pypdf>=4.0.0
python-docx>=1.1.0

# Dev / test
pytest>=8.0.0